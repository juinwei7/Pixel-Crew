# 本機語音輸入規格

> 狀態：草案 v0.1
> 日期：2026-09-01
> 範圍：Pixel Crew 的聊天輸入語音轉文字；本文件僅定義產品與技術規格，不包含實作。
## 1. 背景與目標

Pixel Crew 是本機優先的多 Agent 工作台。使用者目前以文字向 NPC 下達任務；本功能讓使用者可以用繁體中文說話，產生可編輯的聊天文字草稿。

本功能的首要目標是提供可靠、低摩擦且隱私清楚的語音輸入，而不是建立完整的語音助理。

### 成功定義

- 使用者可從 NPC 的主要聊天輸入框開始、停止或取消錄音。
- 30 秒內的口語內容會轉成可編輯的繁體中文草稿。
- 文字只有在使用者沿用既有的送出操作後才會交給 NPC。
- 語音資料預設不離開執行 Pixel Crew 的本機。
- 模型下載完成後，語音轉寫可在離線狀態使用。

## 2. 範圍

### v1 包含

- NPC 主要聊天輸入框的語音轉文字。
- 瀏覽器麥克風權限請求與清楚的狀態提示。
- 錄音、停止、取消、重試，以及保留原本已輸入文字。
- 本機模型的一次性下載、可見的大小資訊與安裝狀態。
- 繁體中文（台灣國語）為預設辨識語言。
- 暫存音訊的安全刪除與失敗清理。

### v1 不包含

- 助理回答朗讀或任何文字轉語音（TTS）。
- 即時逐字稿、語句自動結束偵測、或自動送出訊息。
- 錄音歷史、音訊保存、雲端備份或跨裝置同步。
- Boss Desk、部門討論與其他非 NPC 主要輸入框。
- 遠端／手機存取中的語音輸入。
- 自訂喚醒詞、聲紋辨識或跨 App 聽寫。

## 3. 使用者流程

```text
使用者點選麥克風
  → 首次使用：顯示本機模型說明與下載確認
  → 首次錄音：瀏覽器請求麥克風權限
  → 顯示錄音中、計時與取消控制
  → 使用者停止錄音
  → 音訊只交給 localhost 的轉寫服務
  → 本機語音模型回傳繁中草稿
  → 草稿填入原本的聊天輸入框
  → 使用者可編輯、清除或按既有送出鍵
```

### 行為規則

- 錄音不得覆蓋使用者已輸入的文字；轉寫結果應以可預期方式插入或附加。
- 取消錄音或取消轉寫時，不得改動原本輸入文字。
- 轉寫完成不得自動送出訊息。
- NPC 忙碌時，功能遵守既有待送訊息佇列行為；語音只負責產生文字草稿。

## 4. 架構與本機部署

### 建議基線

採用 `whisper.cpp` 執行多語言 Whisper 模型，作為本機語音轉文字引擎。選擇理由：可離線部署、跨 macOS／Windows／Linux，且 Apple Silicon 有 Metal／Core ML 加速路徑。

```text
Web 聊天頁面
  → 瀏覽器錄音
  → Pixel Crew 的 localhost API
  → 本機 Whisper 推論程序
  → 純文字轉寫結果
  → 原聊天輸入框
```

### 模型策略

- 模型不應打包進 Pixel Crew 主安裝檔。
- 首次啟用時才下載，下載前必須顯示模型名稱、預估大小、儲存位置與可離線使用的說明。
- 首發使用「標準」多語言模型，目標額外磁碟占用約 0.5–1 GB；不得選用英文專用模型。
- 後續可提供「輕量／標準／高精度」選項，但 v1 不必提供使用者切換介面。
- 模型檔存放在 Pixel Crew 的應用程式資料目錄，不得放入使用者選定的工作 repository。

### 併發與資源規則

- 同一個本機服務同時只處理一個語音轉寫工作；其他請求應明確顯示等待或拒絕原因。
- 使用者停止、取消、關閉頁面或服務重啟時，應終止仍在進行的轉寫並清除暫存。
- 模型載入與推論不得阻塞既有的 CLI 工作階段、WebSocket 串流或一般聊天送出。

## 5. 隱私與安全

- 錄音、轉寫與模型推論預設全部在本機完成；不使用雲端語音 API。
- 暫存音訊只在處理期間存在，成功、失敗或取消後立即刪除。
- 原始音訊不得寫入 SQLite、聊天紀錄、診斷資料、備份檔或 log。
- 診斷資料最多記錄匿名、聚合的結果（例如成功／失敗、處理耗時）；不得記錄音訊或轉寫全文。
- 遠端／手機存取下不顯示或不啟用語音輸入。此限制避免遠端裝置的音訊傳到主機後仍被誤解為「裝置本機處理」。
- 模型下載應具備來源、版本與完整性驗證；失敗或中斷時不得留下可被誤用的不完整模型。

## 6. 錯誤處理

| 情境 | 產品行為 |
| --- | --- |
| 未下載模型 | 顯示模型說明與下載動作，不開始錄音。 |
| 模型下載失敗 | 顯示可重試原因；不影響文字聊天。 |
| 麥克風權限遭拒 | 解釋需在瀏覽器／系統設定開啟，保留文字輸入功能。 |
| 沒有偵測到語音 | 顯示可重試提示，不新增空白訊息。 |
| 轉寫失敗或逾時 | 保留原有文字與可重試錄音控制，不送出任何訊息。 |
| 使用者取消 | 立即停止處理、刪除暫存，不改動原有文字。 |
| 本機資源不足 | 提示切換較輕量模型（後續能力）或停用語音；不影響主程式。 |

## 7. 驗收條件

### 功能

- 已安裝模型時，使用者能以開始、停止與取消完成一次語音輸入。
- 使用者在轉寫後可修改文字，且只有按既有送出鍵後訊息才會送往 NPC。
- 取消或失敗不會清空已輸入的文字，也不建立空白待送訊息。
- 離線狀態下，只要模型已完成下載，仍能完成轉寫。

### 資料與隱私

- 對成功、失敗及取消三種流程，驗證處理結束後沒有殘留音訊暫存檔。
- SQLite、備份、WebSocket 事件與一般 log 均不含音訊資料或轉寫全文。
- 在遠端存取情境，功能不可使用並有明確原因。

### 品質與效能

- 使用台灣國語、英文與程式／產品名稱混說、不同音量與一般環境噪音的測試樣本評估結果。
- 對 30 秒內語音，在首發硬體基線上，停止錄音至草稿可用的目標時間為 3 秒內；無法達成時需記錄硬體條件與模型選擇，調整基線後再承諾。
- 語音功能執行期間，既有 NPC 串流與一般文字聊天仍可正常使用。

## 8. 分階段交付

1. **規格確認**：確認硬體基線、模型大小、安裝流程與隱私文案。
2. **v1 實作**：NPC 輸入框、本機模型管理、轉寫服務、清理與錯誤流程。
3. **驗證**：功能、離線、資料清理、性能與繁中品質測試。
4. **v1 發布評估**：確認安裝包增量、模型下載成功率與使用體驗。
5. **後續版本**：擴展到 Boss Desk／部門輸入框，再評估即時逐字稿與遠端情境。

## 9. 待決策項目

| 項目 | 建議預設 | 需確認原因 |
| --- | --- | --- |
| 首發平台 | macOS 與 Windows | 影響模型引擎封裝與效能基線。 |
| 最低硬體 | 待定 | 影響可選模型、轉寫速度與記憶體需求。 |
| 預設模型 | 多語言標準模型 | 要在繁中品質、首次下載與硬碟占用間取捨。 |
| 輸入框覆蓋範圍 | 先限 NPC 對話 | 先降低流程與測試矩陣，再擴展。 |
| 遠端語音 | v1 明確不支援 | 需要獨立的傳輸、權限與隱私設計。 |

## 10. 本機驗證記錄（Spike，2026-09-01）

在本機（Apple M3 Pro / macOS 26.5.2，Metal 加速）用 `whisper.cpp`（`whisper-server`，常駐模式）實測，音源以 `say -v Meijia` 產生繁中＋英文技術詞混講的合成語音（非真人錄音，僅驗證延遲與模型行為，不驗證真實麥克風雜訊情境）。

### 延遲：目標可達成

- 模型「已載入、常駐」後單次請求延遲：15–25 秒音訊約 **1.0–1.6 秒**（`small`／`medium` 皆遠低於 3 秒目標）。
- 冷啟動（模型載入、Metal shader 編譯）約需數秒到十幾秒，屬於「服務啟動」一次性成本，不算進單次轉寫延遲——前提是本機服務要在使用者第一次錄音前就把模型載入好、保持常駐，而不是每次請求都重新啟動子行程。此點目前 §4 沒寫清楚，建議補進「架構與本機部署」。

### 模型大小 vs. 辨識品質：與 §4 的 0.5–1GB 目標有衝突

- **`small`（465MB，落在 spec 建議區間）**：一般語句可用，但把「Claude」聽成「clock／Cloud」、「Codex」聽成「call dex」——這兩個詞是 Pixel Crew 的核心操作對象，錯誤率不可接受。
- **`medium`（1.5GB，超出建議區間）**：同樣測資下「Claude Code」「Codex」「MCP server」「Worker」「pull request」「race condition」等產品/技術詞全部正確。
- 換句話說：目前寫的「0.5–1GB、標準多語言模型」這個預設，在「必須聽得懂自己產品名字」這個最基本的驗收情境下，實測不通過。§9 的「預設模型」建議需要重新確認，而不是維持原預設。

### 找到一個可能的緩解手段：`initial_prompt` 詞彙提示

- whisper 支援在請求時帶一段提示文字（列出領域詞彙，如「Pixel Crew、Claude Code、Codex、pull request、review comment、race condition、WebSocket」），會顯著降低專有名詞誤聽率，且幾乎不增加延遲（實測仍在 0.5–1.6 秒）。
- 加了提示後，`medium` 模型的錯誤幾乎清零；`small` 模型仍會把「Claude」聽成「Cloud」——即使有提示也修不回來。**目前證據不足以支持維持 small 模型當預設**，除非能接受「Claude」這個詞持續被誤聽。
- 這個提示詞可以是固定的產品詞彙表（無需依賴聊天內容動態產生），建議在 §4 模型策略新增一項：轉寫請求應帶固定領域詞彙的 `initial_prompt`。

### 附帶發現：輸出腳本（簡體／繁體）不穩定

- 同一顆 `medium` 模型，語言設 `zh` 時，有時輸出簡體、有時輸出繁體，不可預期；但只要有帶 `initial_prompt`（提示詞本身用繁體寫），輸出穩定變成繁體。
- 保守做法：轉寫服務仍應在回傳前跑一次簡轉繁正規化（例如 OpenCC，本機測試用 `opencc-python-reimplemented` 驗證可用，Node 生態有對應套件），不要只依賴提示詞側的隱性效果，避免行為隨模型版本改變而漂移。§4／§7 建議補上這條正規化步驟。

### 尚未驗證（此次 spike 範圍之外）

- 真人麥克風錄音（含背景噪音、口音、麥克風品質差異）——本次全部是 TTS 合成語音，品質評估上限被高估，真實錄音的辨識率可能更差。
- Windows 平台的模型引擎打包與效能基線（本次只測 macOS + Metal）。
- 低階硬體（無 Metal／無獨立 GPU）的延遲基線。
