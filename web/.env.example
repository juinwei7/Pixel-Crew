# Development uses the Vite proxy and production uses the current origin.
# Only set these when intentionally using another loopback port.
# VITE_SERVER_URL=http://localhost:8787
# VITE_WS_URL=ws://localhost:8787
