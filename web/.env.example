# These overrides are read only by the development build. Production always
# uses the current page origin because Pixel Crew serves its own web bundle.
# Only set these when intentionally using another loopback port.
# VITE_SERVER_URL=http://localhost:8787
# VITE_WS_URL=ws://localhost:8787
